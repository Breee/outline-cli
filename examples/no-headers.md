# No Headers At All

This file has no metadata headers. It requires `--collection-id` on the CLI:

```bash
./outline push --collection-id test --path examples/no-headers.md
```

The title comes from the H1 above.
